extends ../base/base.pug

block content
  +breadcrumb([{ link: '/home.html', name: 'Trang chủ' }, { link: '#', name: 'Tin tức' }])
  section.blog.mb-30px
    .container
      .row 
        .col-12.col-lg-3
          include ../partials/blog-sidebar.pug
        .col-12.col-lg-9
          each article in articles
            +article(article)
          include ../partials/pagination.pug